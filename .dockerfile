# the intent is to include:
# python 3.8.5
# whatever libraries the python stuff depends on (will use a vm to figure this out)
# mongodb 4.4
# if necessary post-compilation, the react/node dependencies